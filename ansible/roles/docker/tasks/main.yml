- name: Install Docker packages
  apk:
    name:
      - docker
      - docker-cli-compose
      - docker-openrc
    state: present
    update_cache: true

- name: Enable and start Docker service
  service:
    name: docker
    state: started
    enabled: true
